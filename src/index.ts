/**
 * Main Event Loop - AIS (AI Voice Agent)
 * Orchestrates the full conversation flow described in the simulation.
 */
async function bootstrap() {
  console.log("Balbine AI Agent started. Waiting for signals...");
}

bootstrap().catch(console.error);