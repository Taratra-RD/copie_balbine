services:
  - type: web
    name: balbine-voice-agent
    plan: free
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: OPENAI_API_KEY
        value: ""
      - key: PUBLIC_BASE_URL
        value: ""
